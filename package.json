{
  "name": "youtube-insight-analyzer",
  "version": "1.0.0",
  "description": "YouTube Insight Analyzer with enhanced visualization features",
  "scripts": {
    "install:all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "start:dev": "concurrently \"cd backend && python app.py\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "test": "cd frontend && npm test"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
