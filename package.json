{
  "name": "youtube-insight-analyzer-enhanced",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "start": "node dist/index.js",
    "deploy": "npm run build && npm run start",
    "lint": "eslint . --ext .ts",
    "prebuild": "npm ci && npm run lint && npm test"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/jest": "^29.0.0",
    "@types/mocha": "^10.0.0",
    "typescript": "^4.9.0",
    "ts-node": "^10.9.0",
    "@types/express": "^4.17.0",
    "@types/websocket": "^1.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0"
  },
  "dependencies": {
    "express": "^4.21.2",
    "websocket": "^1.0.34"
  }
}
